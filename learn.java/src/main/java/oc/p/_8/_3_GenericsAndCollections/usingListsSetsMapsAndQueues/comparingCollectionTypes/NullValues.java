package oc.p._8._3_GenericsAndCollections.usingListsSetsMapsAndQueues.comparingCollectionTypes;

/**
 * The data structures that involve sorting do not allow nulls.
 *
 * TreeSet cannot contain null elements
 *
 * TreeMap cannot contain null keys. Null values are ok
 *
 * ArrayDeque: can't put null because poll() and peek() use null as special return type
 *
 * Hashtable doesn't allow null keys or values (old code)
 */
class NullValues {
}
