package books.learning.RxJava.chapters._6_Concurrency_And_Parallelization.understabdingSchedulers.single;

/**
 * When you want to run tasks sequentially on a single thread,
 * you can invoke Schedulers.single().
 * This is backed by a single-threaded implementation appropriate for event looping.
 * It can also be helpful to isolate fragile, non-threadsafe operations to a single thread
 */
class L {
}
