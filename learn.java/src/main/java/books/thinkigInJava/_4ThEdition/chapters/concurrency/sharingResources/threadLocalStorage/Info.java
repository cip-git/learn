package books.thinkigInJava._4ThEdition.chapters.concurrency.sharingResources.threadLocalStorage;

/**
 *public class ThreadLocal<T> extends Object
 *
 *
 * This class provides thread-local variables.
 * These variables differ from their normal counterparts
 * in that each thread that accesses one (via its get or set method)
 * has its own, independently initialized copy of the variable
 */
class Info {
}
